---
sidebar_position: 2
description: Installation guide for PeriFlow CLI.
---

# 💾 Installation

You can simply install `periflow-client` package using `pip`.

```bash
pip install periflow-client
```

:::info
`periflow-client` requires **python>=3.8**.
:::

:::info
We recommend using the most up-to-date package.
You can check the release history at [PyPI](https://pypi.org/project/periflow-client/#history) and [Github](https://github.com/friendliai/periflow-client/releases).
You can update the package with:

```bash
pip install periflow-client -U
```
:::

:::info
If you have a Hugging Face checkpoint and want to convert it to a PeriFlow-compatible format, you need to install the package with the necessary machine learing library (`mllib`) dependencies. In this case, install the package with the following command:

```sh
pip install periflow-client[mllib]
```
:::
