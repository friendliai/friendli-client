# Detokenization

<a href="https://github.com/friendliai/periflow-client/tree/main/periflow/sdk/api/detokenization.py">
    <img src="https://img.shields.io/badge/View%20source%20on%20GitHub-181717?style=for-the-badge&logo=Github" />
</a>

# _class_ `Detokenization()`

> Bases: BaseAPI

PeriFlow detokenization API.

## Methods

### _def_ `create`

```python
create(
    tokens: List[int]
) -> str
```

Create a detokenization result.

#### Arguments

| Name | Type | Description | Default | Optional |
|------|------|-------------|---------|----------|
| `tokens` | `List[int]` | A list of input token IDs. | - | ❌ |

#### Returns

| Type | Description |
|------|-------------|
| `str` | A detokenized text string. |

#### Raises

| Type | Description |
|------|-------------|
| `APIError` | Raised when the input has invalid format. |

### _async def_ `acreate`

```python
acreate(
    tokens: List[int]
) -> str
```

Create a detokenization result asynchronously.

#### Arguments

| Name | Type | Description | Default | Optional |
|------|------|-------------|---------|----------|
| `tokens` | `List[int]` | A list of input token IDs. | - | ❌ |

#### Returns

| Type | Description |
|------|-------------|
| `str` | A detokenized text string. |

#### Raises

| Type | Description |
|------|-------------|
| `APIError` | Raised when the input has invalid format. |

#### Description

:::info
You must open API session with `api_session()` before `acreate()`.
:::
