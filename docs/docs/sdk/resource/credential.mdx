# Credential

<a href="https://github.com/friendliai/periflow-client/tree/main/periflow/sdk/resource/credential.py">
    <img src="https://img.shields.io/badge/View%20source%20on%20GitHub-181717?style=for-the-badge&logo=Github" />
</a>

# _class_ `Credential()`

> Bases: ResourceAPI[V1Credential, UUID]

PeriFlow credential resource API.

## Methods

### _def_ `create`

```python
create(
    cred_type: CredType,
    name: str,
    value: Dict[str, Any]
) -> V1Credential
```

Creates a new credential.

#### Arguments

| Name | Type | Description | Default | Optional |
|------|------|-------------|---------|----------|
| `cred_type` | `CredType` | The type of credential. | - | ❌ |
| `name` | `str` | The name of credential to create. | - | ❌ |
| `value` | `Dict[str, Any]` | Values of credential in plaintext. | - | ❌ |

#### Returns

| Type | Description |
|------|-------------|
| `V1Credential` | The created credential object. |

#### Raises

| Type | Description |
|------|-------------|
| `InvalidConfigError` | Raised if `value` does not match the credential schema. |

### _def_ `list`

```python
list(
    cred_type: Optional[CredType] = None
) -> List[V1Credential]
```

Lists credentials.

#### Arguments

| Name | Type | Description | Default | Optional |
|------|------|-------------|---------|----------|
| `cred_type` | `Optional[CredType]` | Filters by credential types. Defaults to None. | `None` | ✅ |

#### Returns

| Type | Description |
|------|-------------|
| `List[V1Credential]` | A list of retrieved credentials. |

### _def_ `edit`

```python
edit(
    id: UUID,
    name: Optional[str] = None,
    value: Optional[Dict[str, Any]] = None
) -> V1Credential
```

Edits credential info.

#### Arguments

| Name | Type | Description | Default | Optional |
|------|------|-------------|---------|----------|
| `id` | `UUID` | ID of credential to edit. | - | ❌ |
| `name` | `Optional[str]` | Name to update. No update if it is `None`. Defaults to None. | `None` | ✅ |
| `value` | `Optional[Dict[str, Any]]` | The new value of credential in plaintext. No update if it is `None`. Defaults to None. | `None` | ✅ |

#### Returns

| Type | Description |
|------|-------------|
| `V1Credential` | Updated credential object. |

### _def_ `delete`

```python
delete(
    id: UUID
) -> None
```

Deletes a credential.

#### Arguments

| Name | Type | Description | Default | Optional |
|------|------|-------------|---------|----------|
| `id` | `UUID` | ID of credential to delete. | - | ❌ |
